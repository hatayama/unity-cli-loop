namespace io.github.hatayama.UnityCliLoop.Tests.Editor.HotReload
{
    /// <summary>
    /// Compiled enum used to assert the warning and failure hint when a hot reload adds an enum
    /// member and a patched body names it.
    /// </summary>
    public enum HotReloadSiblingEnum
    {
        First = 1,
        Second = 2
    }
}
